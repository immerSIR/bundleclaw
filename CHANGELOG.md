# Changelog

## 0.1.0
- Initial dual-runtime release scaffold
- Node CLI (npm/npx): export/import/verify/transfer/bootstrap
- Python CLI (pip/uv): export/import/verify/transfer/bootstrap
- Shared bundle format v1 (`.bcz`)
- Export profiles: full, no-credentials, memory-only
- Optional bundle encryption via passphrase
